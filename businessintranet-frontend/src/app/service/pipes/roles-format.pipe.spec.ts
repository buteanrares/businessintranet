import { RolesFormatPipe } from './roles-format.pipe';

describe('RolesFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RolesFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
